description "nodejs-redis-example"

start on filesystem
stop on runlevel [!2345]

env NODE_ENV=production
env REDIS_HOST={{ hostvars['db'].internal_ip }}


respawn

chdir "{{ project_root }}/current"

script
    exec npm start  2>&1  > /dev/null
end script
